---
marp: true
paginate: true
size: 16:9
header: '专利评审答辩 · 基于装载进度的挖掘机卸载点动态选取方法与系统'
footer: 'v5.1 · 2026-09-16 · 据评审意见修订'
style: |
  section { font-size: 22px; }
  h1 { font-size: 40px; color: #1a2f8f; }
  h2 { font-size: 34px; color: #1a2f8f; border-bottom: 3px solid #888; padding-bottom: 6px; }
  table { font-size: 17px; }
  strong { color: #b30000; }
  code { font-size: 16px; }
---

<!-- _paginate: false -->
<!-- _header: '' -->

# 一种基于装载进度的<br>挖掘机卸载点动态选取方法与系统

### 专利评审答辩（模板版）

**交底书版本**：v5.1（据评审/领导意见细化，2026-09-16）
**技术领域**：无人矿山 / 挖掘机自主装车 / 卸载决策
**最接近现有技术**：CN109902857B（国内）· US6363632B1（国际，已过期）
**本轮修订**：概念→公式化建模 · 真实阈值表 · 细化流程图与判定分支 · 异常处理错误码 · 量化效果 · 多机制联动主线 · 卸载效果图 · 泄密脱敏（待康院审）

汇报人：______　　日期：2026-09-16

---

## 本轮修订：对评审意见的回复

> 评审结论：**建议继续推进申请**。两条建议已全部落地到交底书 v5.1，本模板版相应细化如下。

| 意见 | 核心要求 | 本 PPT 落实 |
|:---|:---|:---|
| **建议①** | 补**流程图**（判断条件与分支写明确）、加**卸载效果图**、**泄密待康院审** | 「判断分支与异常」页 +「卸载效果图」页 +「泄密与脱敏」页 |
| **建议②-a** | "远密近疏/双重门限/斗数双向保底/三段式映射"概念 → **公式、形式化建模** | 「机制细化：公式与阈值」页 +「三源联动」页形式化命题 |
| **建议②-b** | 补**算法、阈值、异常处理、量化效果** | 真实阈值表 +「异常错误码」+「量化效果」页 |
| **建议②-c** | "按进度选点"单一创新点偏弱 → **突出多机制联动** | 「三源联动」页（创造性主线上移为反向否决闭环） |

**原则**：所有阈值/公式均来自真实工程代码（`UP.m`/`unloadP.m`/`dump_trajectory.yaml`），**未编造数据**；感知类阈值无代码默认值，已标注"现场标定【待填】"。

---

## 背景技术

**对现有技术情况的介绍（可结合附图说明，现有专利的缺点）：**

挖掘机自动装车中"卸载点选在哪里"，现有技术主要有六类做法：固定点位、人工指定、料位/满斗率检测辅助、轨迹约束点、**感知驱动卸载点序列规划**（US6363632B1、CN109902857B）、**网格覆盖式预规划**（CN114164877B）。其中最接近的现有专利 **CN109902857B** 以单激光雷达点云**深度直方图**表征车厢凹凸并"寻找合适的卸载点"，按额定载荷百分比阈值切换**装满/重心两阶段优先**、并在铲斗**安全卸载范围**内选点（表 1）。但上述做法存在以下缺点（表 2）：

1）**选点与可达域脱节**：CN109902857B 仅有基于铲斗宽度比例的几何安全范围，CN114164877B 的逆运动学仅用于执行到位，卡车停偏时选点仍可能不可达；
2）**判满判据缺失或单一**：深度直方图/整车总重均无"某候选点是否已满"的点级判据，点云稀疏或尖峰噪点易误判；
3）**单一感知源定终止**：一次误判即提前终止或持续超装，无计数交叉校验；
4）**布料顺序非闭环**：预规划顺序重复或按整车载荷百分比切换原则，易近端堆高、铲斗越料；
5）**与执行层脱节**：选点结果不直接衔接卸载轨迹。

---

## 本发明提出的技术方案

以"**可达域 ∩ 未满域**驱动动态选点、并以**斗数双向保底**兜底"为本方案的基本策略。具体是通过在车厢中心线生成远密近疏候选点、逐点逆运动学筛选定可达域边界、点云双重门限判满、自远及近遍历取首个未满点、斗数双向保底校验的七步闭环（图 1）。

```
 步骤1 候选点生成        厢中心线 N 点(优选21)，远密近疏 t=1-(1-t)^k，按 RTK 位姿变换
   ↓
 步骤2 逐点 IK 筛选  ★   主解+固定斗角保底解+限位余量(2°) → 可达域边界(最远可达索引)
   ↓
 步骤3 矿卡可达性判定     最远可达索引 vs 0.5N（含"阈值位已装满豁免"）
   ↓
 步骤4 双重门限判满  ★   点数门限 + 局部平均高度门限 + 满载状态跨轮继承
   ↓ （点云不可用 → 步骤4′ 装载次数三段式映射：远/中/近）
 步骤5 由远及近遍历  ★   取第一个"未满"的可达点
   ↓
 步骤6 斗数双向保底  ★   上限强制终止 / 下限不采信全满 / 中位点兜底
   ↓
 步骤7 调度与轨迹衔接     回转圆 ∩ 车厢矩形 → 入厢衔接点
```

> 各步骤的**公式化建模、真实阈值、判断分支**见后续「机制细化」两页（回应评审建议②）。

---

## 本发明提出的技术方案（三源联动 · 创造性主线）

```
        可达域(几何/运动学)      料面状态(感知)        装载斗数(计数)
              │ 逐点IK筛         │ 双门限判满          │ 双向保底
              ▼                  ▼                    ▼
          [可达候选点]  ∩  [判定未满点]  →  自远及近取首个  →  卸载点
              │                  │                    │
              └──── 三类信息源彼此独立、互为冗余、交叉校验 ────┘
   下限保底：全部判"已满" 但 斗数≤下限 → 不采信该结论，取中位可达点继续
```

> **回应建议②-c（单一创新点偏弱）**：创造性**不在"按装载进度动态调整卸载点"这一上位思想**（与 CN109902857B/CN114164877B 重合），而在下述**三源因果闭环 + 计数对感知的反向否决**。

**形式化命题**：设可达域 `R`（IK 筛选）、未满域 `U`（点云双门限）、计数约束 `C`（斗数双向保底），则 `p* = argfirst_{i∈R, 由远及近}(i∈U)`，再经 `C` 修正——`U=∅ 且斗数≤下限`时**否决"满"结论**（防误停）、`斗数>上限`时**强制终止**（防超载）。三源**独立、交叉校验、相互否决**，区别于"单一感知源判满即终止"的现有技术。

---

## 机制细化①：四个概念 → 公式与真实阈值（回应建议②-a）

| 概念 | 形式化建模（与代码逐行一致） | 来源 |
|:---|:---|:---:|
| 远密近疏 | `t=1-(1-t_raw)^k，k=2.5`；`p_i=p_near+t·(p_far-p_near)` | unloadP.m V2.0 |
| 逐点 IK | `swing=(atan2d(y,x)+180)%360`；余弦定理解 boom/arm，`bucket=α-boom-arm`；主解失败→固定斗角 30°/25° 保底；限位余量 δ=2° | UP.m |
| 双重门限判满 | 圆域 `Ω_i={‖p_xy−g_i‖<r_s}`；`n_i≤n_th→未满`；`n_i>n_th 且 h̄_i≤z_plane+Δh_full→未满，否则已满` | UP.m |
| 斗数双向保底 | `LoadNums>Max→取中位点终止`；`全满 且 LoadNums≤Min→不采信全满、取中位点继续` | UP.m |
| 三段式映射(降级) | `load_num<3→远；<6→中；<9→近；≥9→中(兜底)` | unloadP.m V2.0 |

| 阈值类别 | 取值（三类来源，不臆造） |
|:---|:---|
| **代码固定** | N=21；δ=2°；θ=0.5N=10.5；k=2.5；T=3/6/9 斗；保底斗角 30°/25°；R_coff=0.5 |
| **配置默认** | 机型几何 6.987/2.797/2.359 m；限位 [-25,50]/[-150,-30]/[-135,30]°；车厢 5.0×2.6 m |
| **现场标定【待填】** | n_th、r_s、Δh_full、Δh_pc、Δ_j、h_up、Max/MinLoadNum（Simulink 运行时输入，无代码默认） |

> **实现事实**：判满+双向保底在 `UP.m`、远密近疏+三段式在 `unloadP.m` V2.0，无单一文件同含两者 → 主张**组合实施例**（已如实标注）。

---

## 机制细化②：判断分支与异常处理（回应建议①·②-b）

**关键判定条件 - 分支 - 处理**：

| 环节 | 条件 | 分支处理 |
|:---|:---|:---|
| IK 可解 | `\|cosβ\|≤1 且 \|cosα_\|≤1`、关节∈[限位±δ] | 否→保底解重判；再否→剔除该点 |
| 矿卡可达 | `idx_far>10.5`，或`阈值位已装满` | 满足→可达；否则→请求重新停靠 |
| 判满·门限1 | `n_i ≤ n_th`（稀疏） | **保守判未满**（不算高度） |
| 判满·门限2 | `n_i>n_th` 且 `h̄_i≤z_full` | 未满；`h̄_i>z_full`→已满（局部均值抑尖峰） |
| 保底·上限 | `LoadNums > Max` | 取中位点收尾（防超载） |
| 保底·下限 | 全满 且 `LoadNums ≤ Min` | **不采信全满**、中位点继续（防误停） |

**异常处理与错误码（真实代码）**：`CheckReachable` 失败→**error_code=1**（卡车位置无法卸载）；航路点生成失败→**error_code=2**（轨迹规划失败）；运行期中止→**error_code=3**（区别正常完成 FinishFlag=1.0）；段完成需连续 **3 帧**确认（防液压振荡）；段超时（预期×2.0）强制推进；铲斗高度门控未达 2.0+1.8 m → 4 s 后提升动臂 3°、最多 6 次；clamp 修正量>5° 终止激活；总超时 30 s。**任何异常路径均输出中位可达点及关节角——系统永不无解。**

---

## 量化效果（结构性保证 + 待测占位，不编造）

**A. 结构性可保证（由算法构造直接成立，可论证）**：

| 指标 | 保证方式 | 结论 |
|:---|:---|:---:|
| 卸载点可达率 | 仅从 IK 可解+限位余量的可达集合选点 | **100%（构造性）** |
| 稀疏 / 尖峰误判 | 门限1 保守判未满 / 门限2 用局部均值 | 两类误判**方向相反各挡一类** |
| 结论抖动 | 满载跨轮继承 | 相邻轮不翻转 |
| 误停 / 超载 | 下限 / 上限双向保底 | 双向防护 |
| 无解率 | 所有 return 输出中位点 | **0（永不无解）** |

**B. 待实机测试填充（占位，未编造数据）**：停偏工况选点成功率、单斗判满准确率/误判拦截次数、方量利用率/满载率、感知失效降级可用率——均为**【待测试填充】**。

> **获得真实数据前不填任何数值**，避免不可靠量化表述进入专利稿。

---

## 卸载效果图（回应建议①·效果图）

**示意图（车厢俯视）**：

```
   车头 ←──────── 车厢中心线（N=21，远密近疏） ────────→ 车尾
   [远端·密]  ●●●●●●     · · ·      ○ ○  ○   ○    ○   [近端·疏]
             └ 已满(继承) ┘  └已满┘   ↑首个未满 = 当前卸载点 p*
   可达域边界 idx_far ───────┘（> 0.5N → 矿卡可达）
   布料方向：由远及近 ▶▶▶ ；可达域 ∩ 未满域选点，斗数双向保底兜底
```

- 红●=已满（含跨轮继承）；**p\***=由远及近遍历命中的首个未满点；灰○=未满候选点；虚线=可达域边界。
- **真图（占位）**：RViz / 点云实拍截图【待补，建议脱敏处理，**待康院审核**】。

> 矢量示意版见交底书 v5.1 图 9a（内联 SVG）。

---

## 对比分析最接近的现有技术与本发明

CN109902857B　一种运输车辆装载点自动规划方法及系统（江苏徐工工程机械研究院有限公司）

本申请涉及一种基于装载进度的挖掘机卸载点动态选取方法及系统。最接近的现有专利 CN109902857B 公开了：使用一台激光雷达同时扫描铲斗与车厢，对车厢做**深度直方图**表征凹凸、在"较深较大的凹区域"寻找卸载点，按额定载荷百分比阈值切换**装满/重心两阶段优先**，并在铲斗**安全卸载范围**（车沿距离限制）内选点。其以单一感知源决定装载，未给出点级判满判据，也无运动学可达域筛选与计数保底。

| | 相同点 | 不同点 | 建议 |
|:---|:---|:---|:---|
| 现有专利 | 均以激光雷达点云感知车厢料面、据此在车厢上方自动选取卸载点，解决布料不均/撒料 | 仅深度直方图+凹区域选点；几何安全卸载范围；按额定载荷%两阶段优先；单一感知源判满终止 | 具有新颖性与创造性，建议申请 |
| 本提案 | （同上） | **逐点逆解可解性筛选定可达域边界**＋**点级双门限判满（点数不足保守判未满）**＋**自远及近遍历取首个未满点**＋**斗数双向保底** | |

> 另经全文复核：US6363632B1（已过期）仅公开"感知料面→逐斗迁移卸载点"上位思想；CN114164877B、US12216018B2 为次级结合文献——均不破坏上表"本提案"不同点的机制组合。

---

## 本发明的优点

1、选出的卸载点**必然可达**，卡车停靠偏差自适应，停偏时在缩小后的可达域内自动选点；

2、布料与实际料面**闭环**，车厢由远及近均匀装满，避免局部堆高、撒料与铲斗越料；

3、**双重门限**拦截稀疏与尖峰两类典型误判，判满结论稳定、不抖动；

4、感知与计数**互为冗余**，单传感器失效不中断作业，杜绝误停半车与感知失灵超载；

5、系统**永不无解**，任何异常均输出中位可达点兜底，且选点与卸载轨迹一体化衔接。

---

## 关键点和保护点（领导最关注点）

需要保护的技术创新点为以下**互为因果、不可拆分**的组合机制（独立权利要求 1 必要技术特征）：

**①** 车厢中心线 N 点**远密近疏**候选点生成＋按矿卡实测位姿变换；
**②** 逐候选点**逆运动学可解性筛选**并据此确定**可达域边界**；
**③** **点级双重门限判满**（点数门限＋局部平均高度门限＋点数不足保守判未满）；
**④** **自最远可达点向近端遍历**取首个未满点；
**⑤** **斗数双向保底**——全满但斗数不足时**不采信感知结论**、取中位可达点继续（全案创造性支点）。

**不保护**"感知料面→选卸载点"的上位思想（该思想已被 CN109902857B / US6363632B1 公开）。创造性主线为 ②③④⑤ 的**三源联动 + 计数对感知反向否决**（见「三源联动」页）。

---

## 泄密与脱敏标注（回应建议①·待康院审核）

| 敏感项 | 位置 | 脱敏建议 |
|:---|:---|:---|
| 机型几何精确值（动臂/斗杆/铲斗齿长、偏置、限位） | 阈值表 | 专利需充分公开可保留；敏感则符号化/给区间 |
| **RTK 绝对高程基准**（`ground_protect_alt=2919.562`、`rtk_to_swing_center_alt_offset=1.230`） | `GP2mid_pkg/config/gp_params.yaml` | **建议不写入公开文本**，仅述"以 RTK 高程基准做地面保护"、不给绝对值 |
| 现场标定阈值（n_th、r_s、Δh_full 等） | 阈值表 | 公开文本用符号+建议区间，不给产线精确值 |
| 车厢实测尺寸 / 真实工地坐标 | 效果图真图 | 用示意图或脱敏截图，去除真实坐标/背景 |

> 上述尺度**最终以康院审核为准**；交底书相应处已标【敏感·待审】/【待填】。

---

<!-- _paginate: false -->

## 谢谢，请提问

**结论**：① 技术方案成熟、七步流程与降级路径完整；② **已按评审意见完成 v5.1 细化**——四个概念全部落成**公式与形式化建模**，补**真实阈值表**（三类来源、不臆造）、**判断分支与异常错误码 1/2/3**、**量化效果**（结构性+待测占位）、**卸载效果图**与**泄密脱敏（待康院审）**；③ 检索已定位真实最接近现有技术并据此收窄独权；④ **创造性主线上移**为"三源交叉校验 + 计数对感知反向否决"的多机制联动（回应"单一创新点偏弱"），支点为 ②③④⑤ 组合（尤以 ⑤ 斗数双向保底最强），**具备提交条件**。

**仍待补充**：实机测试数据（量化效果 B 表）· 效果图真图（RViz/点云脱敏）· 现场标定阈值数值 · 康院泄密审核结论。
